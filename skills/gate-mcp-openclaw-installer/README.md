# Deprecated

Use **[gate-mcp-installer](../gate-mcp-installer/)** — OpenClaw / mcporter:

```bash
bash skills/gate-mcp-installer/scripts/install.sh --platform openclaw
bash skills/gate-mcp-installer/scripts/install.sh --platform openclaw --select
```

Legacy scripts in `scripts/` are unmaintained; prefer the unified installer.
